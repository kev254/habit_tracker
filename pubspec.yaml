name: habit_tracker
description: "Habit Tracking By Kevin for Solutech interview"

publish_to: 'none'

version: 1.4.0+3

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.1.1
  path_provider: ^2.1.2
  flutter_slidable: ^3.1.2
  flutter_heatmap_calendar: ^1.0.5
  awesome_notifications: ^0.9.3+1
  workmanager: ^0.5.2
  shared_preferences: ^2.2.2
  sqflite: ^2.0.0+4
  http: ^1.2.2
  google_sign_in: ^6.2.2
  rflutter_alert: ^2.0.7
  flutter_spinkit: ^5.1.0
  get:
  fl_chart: ^0.70.0
  animated_snack_bar: ^0.4.0


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/

